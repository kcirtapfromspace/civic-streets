// WS3: Template adapter — fits templates to target ROW width
import type { StreetSegment, TemplateDefinition } from '@/lib/types';

export function adaptTemplate(
  template: TemplateDefinition,
  targetROWWidth: number,
): StreetSegment {
  // WS3 implements: parametric width adaptation
  void template;
  void targetROWWidth;
  throw new Error('Not implemented — WS3');
}
