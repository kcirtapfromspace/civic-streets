// WS3: Template loader — loads template JSON files
import type { TemplateDefinition } from '@/lib/types';

export function loadTemplates(): TemplateDefinition[] {
  // WS3 implements: load all template JSONs from data/templates/
  throw new Error('Not implemented — WS3');
}

export function getTemplateById(id: string): TemplateDefinition | null {
  // WS3 implements
  void id;
  throw new Error('Not implemented — WS3');
}
